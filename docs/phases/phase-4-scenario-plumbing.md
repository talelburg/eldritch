# Phase 4 — Scenario plumbing

## Status

🟡 In progress. Design pass complete 2026-05-21. First eleven PRs merged: `#103` unified window stack as PR #129, `#74` ScenarioModule skeleton as PR #130, `#72` encounter deck state as PR #132, `#126` Revelation DSL + on-draw resolution as PR #133, `#127` enemy spawn rules as PR #135, `#69` Mythos phase content as PR #136, `#70` Upkeep phase content as PR #142, `#71` Enemy phase engagement attacks as PR #145, `#137` Investigation phase full driver as PR #148, `#128` hunter movement as PR #149, and `#144` investigator elimination flow as PR #152. Remaining: `#73` and `#147` (mulligan player-order cursor, filed as a `#137` follow-up). `#146` (Investigation turn-order choice) was filed as a `#137` follow-up and milestoned to Phase 8.

## Goal

A synthetic toy scenario plays setup → resolution in tests, demonstrating that the engine drives all four phases through real scenario data — encounter deck draws, treachery + enemy resolution, hunter movement, doom progression, and act/agenda transitions.

## Issues (open: 3 — `#73` carried over, plus `#147` and `#150` filed as follow-ups; `#75` migrated to Phase 9, `#146` follow-up milestoned to Phase 8)

| # | Title | Notes |
|---|---|---|
| `#73` | act + agenda + doom + threshold-advance | Kept whole. Doom +1 at Mythos start, threshold-driven agenda advance, act-advance condition emits `ActAdvanced`, end-of-deck → `ScenarioWon` / `ScenarioLost`. `#144` (PR #152) repointed the no-active-investigator park branch's `TODO` to `#73` — wiring `Resolution::Lost` here makes that park unreachable (Rules Reference p.10 step 6). |
| `#150` | Upkeep 4.3: re-engage readied enemies with co-located investigators | Filed as a `#144` follow-up. `ready_exhausted_cards` flips `exhausted` without running the engagement check, so a ready-able + unengaged + co-located enemy never re-engages (RR p.10: "if an exhausted enemy … becomes ready, it engages as soon as it is readied"). Reachable today single-player via a successful Evade (exhausts + disengages) surviving to Upkeep. **Unblocked** by `#144` (PR #152): the fix is one `reengage_at_location` call per newly-readied enemy. Not a Phase-4-done blocker (the synthetic demo doesn't exercise it). |
| `#147` | Mulligan in player order: cursor model | Filed as a `#137` follow-up. Replace `mulligan_window: bool` + per-investigator `mulligan_used` with a `mulligan_pending: Option<InvestigatorId>` cursor mirroring `mythos_draw_pending` (Rules Reference p.16 / p.27: mulligan happens in player order). Overlaps `#137` only at the round-1 kickoff trigger (`mulligan_window` flip → cursor reaches `None`). |

### Closed

| # | Title | PR | Notes |
|---|---|---|---|
| `#103` | unified window stack (player + reaction) | #129 | Foundational refactor of #52 machinery; ships unified `open_windows` stack. |
| `#74` | scenario module skeleton: `ScenarioModule` + `ScenarioRegistry` | #130 | `ScenarioId` / `Resolution` / `ScenarioModule` / `ScenarioRegistry` in `game-core`; synthetic fixture in `scenarios`; engine post-apply hook with parameterized `apply_with_scenario_registry` helper for test mocking. |
| `#72` | encounter deck state | #132 | `GameState.encounter_deck: VecDeque<CardCode>` + `encounter_discard: Vec<CardCode>`. Helpers `shuffle_encounter_deck` / `reshuffle_encounter_discard` / `draw_encounter_top` mirror the existing player-deck pattern in `engine/dispatch.rs`. `EngineRecord::EncounterDeckShuffled` + `Event::EncounterDeckShuffled` are additive siblings to the existing `DeckShuffled` (which stays player-deck-only). |
| `#126` | DSL `Trigger::Revelation` + `EventPattern::CardRevealed` + on-draw resolution | #133 | `Trigger::Revelation` and `EventPattern::CardRevealed { card_type: Option<CardType> }` land in `card-dsl`. Engine: `Event::CardRevealed` + `EngineRecord::EncounterCardRevealed` + `encounter_card_revealed` dispatch handler. Documented exception to validate-first / mutate-second contract (early `Event::CardRevealed` emission is the rules-correct interposition point for Before-timing reactions). Enemy arm stubbed for #127 to flip. First consumer: synthetic treachery in `crates/scenarios/src/test_fixtures/synth_cards.rs` with effect "gain 1 resource" (chose existing `Effect::GainResources` over a new lose-resources primitive — corpus has no in-scope two-consumer case). |
| `#127` | enemy spawn rules | #135 | `Spawn { location: SpawnLocation }` + `Option<Spawn>` on `CardMetadata`; engagement-on-spawn for 0 / 1 investigators (multi rejects, pointing at #128); `EventPattern::EnemySpawned` (bare); `Event::EnemySpawned` extended with `code` + `engaged_with`, paired with `Event::EnemyEngaged` on engagement; new `state.next_enemy_id` counter; `Location.code: CardCode` field; pipeline emits `spawn: None` for every card; synthetic enemy in `synth_cards.rs` proves the wiring end-to-end. |
| `#69` | Mythos phase content (draw + resolve + Surge) | #136 | Player-driven `PlayerAction::DrawEncounterCard` (peer to Investigate / Move / PlayCard, not an `AwaitingInput` sub-choice). Per-card 5-step sub-sequence with surge re-draw loop (`MAX_SURGE_CHAIN = 64` cap + two `unreachable!()` sites for scenario-data malformation). Post-1.4 `MythosAfterDraws` player window via new `open_fast_window` helper (push-before-scan + Skip-closes-pure-Fast). Mythos and skeleton Investigation phase drivers establish the phase-driver-owns-its-boundary-emits pattern (#70 / #71 / future full Investigation driver follow). `start_scenario` skips Mythos entirely in round 1 (no phantom boundary events; rules say "skip" not "run empty"). Extracted `check_play_card` / `check_activate_ability` validators back the Fast-eligibility scan. Status-filter on cursor advance: eliminated investigators (Killed / Insane / Resigned) skip Mythos 1.4 draws AND Investigation 2.2 turn rotation per Rules Reference p.10. Two pre-PR review passes; surge treachery + Fast event added to synth fixture. |
| `#70` | Upkeep phase content | #142 | `upkeep_phase` / `upkeep_resume` / `upkeep_phase_end` driver mirrors `mythos_phase` / `mythos_phase_end` inverted around the (leading) post-4.1 window position; runs 4.2–4.6 through `open_fast_window(UpkeepBegins)`. Action refresh relocated from `rotate_to_active` (now set-active-only) into Upkeep 4.2 (`reset_actions`, emit-on-change); `start_scenario` seeds round 1 — `ActionsRemainingChanged` now fires once per round at Upkeep, not at turn-start. Round-counter increment relocated from `step_phase` into `mythos_phase` step 1.1 (the rule's "round begins" point). 4.3 readies investigator in-play cards (new `Event::CardReadied`) AND exhausted enemies (existing `EnemyReadied`). 4.4 is two-pass over Active investigators (all draws, then all gains). 4.5 hand-size is a `TODO(#111)` stub. Shared helpers `grant_resources` (with DSL `gain_resources`) and `draw_one_with_deckout` (with `Draw` action) extracted. Spec-flagged `BetweenPhases` never-opened-in-production finding routed to #140. |
| `#71` | Enemy phase: engagement attacks | #145 | `enemy_phase` (3.1 + 3.2 `hunter_movement_step` stub for #128 + 3.3 cursor seed + first `BeforeInvestigatorAttacked` window) + `enemy_phase_end` (3.4 + Enemy→Upkeep) mirror the Mythos cursor-driven driver shape. Per-investigator `WindowKind::BeforeInvestigatorAttacked` + final `WindowKind::AfterAllInvestigatorsAttacked` (both bare); cursor on new `GameState::enemy_attack_pending` field (mirror of `mythos_draw_pending`). `resolve_attacks_for_investigator` snapshots engaged ready enemies in `EnemyId` order, fires `enemy_attack` + exhausts each, early-breaks on `Status != Active` (rules-correct minimal interpretation until `#144` lands — full Elimination step 3 disengage + re-engage; `#144` is blocked on `#128` so multi-investigator re-engagement can use prey logic directly). Two shared cursor helpers `first_active_investigator` + `next_active_investigator_after` extracted from Mythos so both Mythos and Enemy share one eliminated-skip path. `step_phase`'s `_` fallback becomes `unreachable!()` now that all four phases are driver-dispatched; `from != Phase::Enemy` joins the `PhaseEnded` suppression set. `end_turn`'s second `step_phase` call removed (the cascade now runs through `enemy_phase` → continuations → `enemy_phase_end` → `step_phase(Enemy→Upkeep)` → upkeep → Mythos in one chain). Deterministic `EnemyId` attack order with `TODO(#143)` at both call sites (`resolve_attacks_for_investigator` + `fire_attacks_of_opportunity`). |
| `#137` | Investigation phase full driver: sub-steps 2.1 / 2.2 / 2.2.1 / 2.2.2 / 2.3 | #148 | Named-step driver: `investigation_phase` (2.1 + opens `WindowKind::InvestigationBegins`), `begin_investigator_turn` (2.2 + opens `WindowKind::InvestigatorTurnBegins`, reused by the `InvestigationBegins` continuation and by `end_turn`), `end_turn` (2.2.2, rotates via `next_active_investigator_after` or ends), `investigation_phase_end` (2.3 → Enemy). Rotation moved into the `InvestigationBegins` continuation to preserve the printed 2.1 → window → 2.2 order. **Phase begins after setup:** round-1 kickoff moved from `start_scenario` to the mulligan-completion site in `apply_player_action` (Rules Reference p.27: no action windows during setup) — `StartScenario` no longer emits `PhaseStarted(Investigation)` or sets `active_investigator`. `step_phase`'s conditional `PhaseEnded` emit deleted (all four phases own theirs). No-active-investigator branch **parks** (cascade-breaker); `TODO(#144)` for the p.10-step-6 scenario-end. Turn-order *choice* (`ChooseFirstActor`) + per-action window re-open deferred to `#146` (Phase 8); mulligan player-order cursor deferred to `#147`. |
| `#128` | Hunter movement | #149 | Split out of `#71`. Runtime `Enemy.hunter: bool` + `Enemy.prey: Prey` (new `card-dsl` enum `Default` + `HighestStat(Stat)`, `#[non_exhaustive]`); BFS helpers (`bfs_distance` / `shortest_first_steps`) over the location-connection graph in `engine/pathfinding.rs`. Step-3.2 driver `drive_hunter_moves` → `process_one_hunter` (move one shortest-path step + engage-on-arrival; co-located enemies don't move per RR p.12). Shared `resolve_prey` (→ `One` / `Tie` / `None`) reused across move-target / engage-on-arrival / engage-on-spawn. Lead-investigator ties suspend via `AwaitingInput`: movement is one `InputResponse::PickLocation` over the prey-filtered shortest-step union, engage-on-arrival one `PickInvestigator`. Cleared `#127`'s deferred multi-investigator engagement-on-spawn (option A — `spawn_engage_pending` suspends the Mythos draw loop through the extracted `run_mythos_draw_chain`, preserving the surge cap budget across the round-trip). `enemy_phase` / `step_phase` / `investigation_phase_end` / `end_turn` now thread `EngineOutcome` so a hunter tie surfaces out of the originating `EndTurn`; attack-loop kickoff extracted to `enemy_attack_kickoff`. |
| `#144` | Formalize investigator elimination flow (RR p.10 steps 1–5) | #152 | `apply_investigator_defeat` calls new `run_elimination_steps`: step 1 drains `cards_in_play`/`hand`/`deck`/`discard` into new `Investigator.removed_from_game`; step 2 places clues on the `Location` (`LocationCluesChanged`) + zeros resources; step 3 disengages engaged enemies (`EnemyDisengaged`) then re-engages the **ready** ones via new shared `reengage_at_location` (prey-resolved, **auto-picks the lead on a tie** — no suspension, so the defeat path stays a synchronous `fn`); then clears `current_location`. Steps 4/5/6 are documented no-ops (no threat-area-card state yet; lead is computed via `first_active_investigator`, not stored; `AllInvestigatorsDefeated` already fires, `Resolution::Lost` is `#73`). Step 3 does **not** write `enemy.current_location` — engaged enemies are already co-located (Move drags them; engage sites set location first). Closed `#71`'s early-break gap (kept as redundant-but-harmless). Filed follow-ups `#150` (Upkeep-4.3 engage-on-ready, reuses `reengage_at_location`) + `#151` (Phase-8 interactive tie). |

### Moved out of Phase 4

| # | Title | New home |
|---|---|---|
| `#75` | campaign log + `Fact` enum + scenario sequencing | Phase 9. Phase 4's `apply_resolution` returns a typed `Resolution` and applies XP/trauma directly; the `Fact` log and `next_scenario` orchestration land alongside Night of the Zealot. |

### Still unmilestoned (concrete-consumer-first)

- `#56` Study (01111) — waits on a "location abilities DSL + reveal effects" issue (also unfiled). Picked up together when location-bearing content is in scope.
- Trigger indexing (perf) — `#52` deferral, resurfaces when boards grow.
- `#143` Player picks engaged-enemy attack order (Enemy phase 3.3 + attacks of opportunity) — `#71` deferral. Concrete consumer = multi-investigator + multi-engagement scenario (likely Phase 7+ / Gathering). Both deterministic-order sites carry `TODO(#143)` markers.
- Fast-eligibility pause test for the Enemy-phase `BeforeInvestigatorAttacked` window — `#71` deferral (the Skip-resume path is tested; the pause-on-Fast path needs a Fast event in hand + card-registry install, which makes it a `crates/cards/tests/` integration test rather than a `game-core` unit test). Inline `TODO(#71 follow-up)` at the test module names the missing fixture. Pulled in alongside the first card-test that exercises Fast play during the Enemy phase. Not filed as a GitHub issue — small enough to fold into that natural future PR.

## Ordering (Shape B)

| # | PR / planned step | Why this slot |
|---|---|---|
| 1 | `#103` unified window stack | ✅ PR #129. Foundational refactor of `#52` machinery. Every subsequent phase-content PR opens windows; doing this first means each plugs into a stable shape rather than retrofitting twice. |
| 2 | `#74` `ScenarioModule` + registry + synthetic fixture stub | ✅ PR #130. Defines the shape every later issue conforms to. Synthetic fixture: 1 location, 1 investigator, one-line resolution predicate (`phase == Investigation && round >= 1`). Engine learns to call `detect_resolution` post-`apply`. |
| 3 | `#72` encounter deck state | ✅ PR #132. Sets up the data Mythos will draw from. Helpers in `crates/game-core/src/engine/dispatch.rs` mirror the existing player-deck shape. |
| 4 | `#126` DSL `Trigger::Revelation` + on-draw path | ✅ PR #133. Lands the DSL primitive in isolation. First consumer is a synthetic treachery in the fixture. |
| 5 | `#127` enemy spawn rules | ✅ PR #135. First consumer is a synthetic spawn-bearing card. Establishes the `Spawn` keyword surface + `spawn_enemy` handler that #69's Mythos draw loop calls into via `encounter_card_revealed`. |
| 6 | `#69` Mythos phase content | ✅ PR #136. Composes 3 + 4 + 5. |
| 7 | `#70` Upkeep phase content | ✅ PR #142. Ready / draw / gain 1; action refresh and round-counter increment relocated to their rules-correct framework steps (Upkeep 4.2 and Mythos 1.1). |
| 8 | `#71` Enemy phase: engagement attacks | ✅ PR #145. Per-investigator + final Fast-windows; deterministic `EnemyId` attack order with `TODO(#143)` at both Enemy-phase and AoO sites; early-break on defeat with the full disengage + re-engage flow filed as `#144` (blocked on `#128` for prey-based multi-investigator re-engagement). Shared cursor helpers extracted from Mythos. `step_phase`'s `_` fallback becomes `unreachable!()`. |
| 9 | `#137` Investigation phase full driver | ✅ PR #148. Closes out the driver cluster — all four phase drivers own their boundary emits; `step_phase`'s conditional `PhaseEnded` emit deleted. Phase begins after the mulligan setup window (kickoff moved to mulligan completion). Turn-order *choice* + per-action window re-open deferred to `#146`; mulligan player-order cursor to `#147`. |
| 10 | `#128` Hunter movement | ✅ PR #149. Prey enum + BFS + PickLocation/PickInvestigator tie resolution; shared `resolve_prey` cleared `#127`'s deferred multi-investigator spawn engagement (option A). Unblocks `#144`. |
| 11 | `#144` Formalize investigator elimination flow | ✅ PR #152. `run_elimination_steps` does steps 1–3 (remove cards → `removed_from_game`; clues→location + resources→0; disengage + ready-only prey re-engage via shared `reengage_at_location`, auto-lead-on-tie). Steps 4/5/6 documented no-ops; defeat path stays synchronous. Closed the `#71` early-break gap. Follow-ups `#150`/`#151`. |
| 12 | `#73` act + agenda + doom + threshold-advance | Wires into Mythos start (doom +1), engine state conditions (act-advance), end-of-deck win/lose events. |
| 13 | Phase-4 closing demo | Synthetic scenario plays setup → resolution end-to-end as an integration test in `crates/scenarios/tests/`. May fold into the previous PR. |

`#72`, `#126`, `#127` are independent of each other and could land in parallel. `#73` is independent of phase content and could slip earlier. `#103` and `#74` could swap, but `#103`-first means `#74`'s engine integration uses the unified window stack directly.

## Decisions made (design pass 2026-05-21)

- **Unified window stack shape (`#103`, PR #129).** `GameState.open_windows: Vec<OpenWindow>` replaces the old `in_flight_reaction_window: Option<ReactionWindow>` shape. Multi-window nesting is structural. Phase-content PRs (`#69` / `#70` / `#71`) plug into this stack via `queue_reaction_window` (push) and `close_reaction_window_at` (remove by index); the `WindowKind` enum is `#[non_exhaustive]`, so adding new variants (`BetweenInvestigatorTurns`, `BeforeEnemyAttack`, etc.) at consumer-PR time is non-breaking.
- **`close_reaction_window_at` takes an explicit window index (`#103`, PR #129).** Phase-content PRs that push pure-Fast-gating windows (`BetweenPhases`, etc.) on top of a draining reaction window MUST resolve the close target via `GameState::top_reaction_window_index()`, not via `last()` / `pop()`. The stack would otherwise corrupt: `top_reaction_window_mut()` skips empty-`pending_triggers` windows but `pop` doesn't, so a `BetweenPhases-on-top-of-ReactionWindow` stack would close the wrong window.
- **Card-level Fast restrictions are NOT engine-enforced (`#103`, PR #129).** The engine's PlayCard gate enforces the type-level Fast rules from Rules Reference page 11 (events: any window where `fast_actors` permits; assets: owner-only). Card-level restrictions like Working a Hunch's "Play only during your turn" are out of scope — a future DSL primitive will enforce them. Don't assume the engine prevents a Fast event from being played by a non-owner when a window permits.

- **Engine resolution hook is a parameterized helper (`#74`, PR #130).** `apply()` is a one-liner over `apply_with_scenario_registry(state, action, scenario_registry::current())`. Engine unit tests pass a locally-constructed mock `ScenarioRegistry` to the parameterized variant; the process-global `OnceLock` is only touched by one test (the idempotent-install test in `scenario_registry`) and by the `scenarios::tests::synthetic_resolution` integration test. The parameterized helper is intentionally **not** re-exported at `game_core`'s crate root — it lives at `game_core::engine::apply_with_scenario_registry`, signalling it's a test-mocking escape hatch rather than a peer to `apply()`. Pattern is the recommended shape for future engine ↔ registry interactions where unit tests would otherwise contend on `OnceLock`.
- **`Resolution` is `Won { id: String } / Lost { reason: String }` (`#74`, PR #130).** String payloads stand in for Phase-9's typed campaign-log `Fact` enum. Both variants kept `#[non_exhaustive]` so Phase 9 can extend without breaking Phase-4 consumers. `id` was chosen over `branch` / `resolution_id` because ArkhamDB's resolution identifiers (`R1` / `R2` / `R3` / `R4`) are conventionally called "resolution IDs" in the source data, and `id` reads cleanly in pattern-match positions (`Won { id }` vs `Won { branch }`). The `#[non_exhaustive]` annotation protects variant shape but not field names — a future rename would be breaking. Worth re-examining when the first real scenario (Phase 7 Gathering) lands enough resolution variants to confirm the name in context.
- **`apply_resolution` is called by the engine right after `ScenarioResolved` (`#74`, PR #130).** Same `apply()` call, same events buffer. Action-log replay reproduces XP / trauma changes deterministically. `apply_resolution` is a `fn` (not `Fn`/`FnMut`), so by signature it cannot reject — Phase 9 inherits the constraint that resolution effects must be infallible at the type level. If Phase 9 needs to surface "couldn't apply trauma because X," it'll need either a degraded `Event::ScenarioResolutionFailed` and continue-anyway, or engine-side pre-validation before the call. Idempotency latch is deferred — see `#131`.
- **`scenarios::test_fixtures` defaults on, including in `server` (`#74`, PR #130).** Empirically necessary: cargo compiles `scenarios` as a normal dependency of `scenarios/tests/*.rs` integration binaries (not with `cfg(test)`), so the `#[cfg(any(test, feature = "test_fixtures"))]` gate would otherwise be inactive there. As a side effect, `crates/server/Cargo.toml` (which has `scenarios = { path = "../scenarios" }` without `default-features = false`) compiles the synthetic fixture into the production binary. Today it's harmless dead code; the cleanup window is **when Phase 7 ships the first real scenario** — at that point `server` should add `default-features = false` and the fixture stays test-only. File-grep anchor: `default = ["test_fixtures"]` in `crates/scenarios/Cargo.toml`.

- **Additive sibling for `DeckShuffled` (`#72`, PR #132).** Encounter deck shuffles ride a new `EngineRecord::EncounterDeckShuffled` / `Event::EncounterDeckShuffled` rather than renaming or tagging the existing `DeckShuffled` (which stays player-deck-only). Trade-off: one variant says "player" implicitly, the other says "encounter" explicitly. Worth re-examining if act / agenda decks join the family — at that point a tagged `DeckKind` refactor becomes load-bearing. Companion convention: the mid-handler reshuffle path (`reshuffle_encounter_discard` called from `draw_encounter_top` on empty deck) does NOT push the `EngineRecord` — mirrors the player-deck pattern where replay determinism comes from the seeded RNG, not log entries. The `EngineRecord` variant is reserved for explicit "shuffle X into the encounter deck" effects.

- **`EventPattern::CardRevealed { card_type: Option<CardType> }` (`#126`, PR #133).** Chose `card_type` narrowing over the `EnemyDefeated`-mirror `by_controller: bool`. Encounter draws are engine-driven, not card-controlled, so `by_controller` doesn't fit the semantics; treachery-vs-enemy narrowing is the load-bearing distinction for hypothetical Forewarned-style listeners. The first real listener (Phase-7+) gets to confirm or extend.
- **`Event::CardRevealed` emits BEFORE Revelation resolves (`#126`, PR #133).** Intentional ordering: Before-timing reaction listeners (#52's machinery; not yet wired) need the event to fire first so they can interpose / cancel. Documented exception to validate-first / mutate-second — the only state-changing pre-emit op is the encounter-deck draw, which is the load-bearing 'reveal' moment per the Rules Reference. Precedent: `play_card`'s mid-resolution caveat in CLAUDE.md. `#127`'s spawn-handler PR retains the same shape (the enemy arm replaces the stub reject, but the reveal-before-spawn ordering stays).

- **Multi-investigator engagement-on-spawn defers to #128 (`#127`, PR #135).** Single-investigator and zero-investigator cases handled per Rules Reference p.10 ("Any time a ready unengaged enemy is at the same location as an investigator, it engages that investigator") and p.24 (default-spawn fallback). The multi-investigator path requires Prey resolution — which shares its shape with hunter-movement target selection in #128 — so rather than build a single-use prey resolver here, engagement-on-spawn rejects with `"spawn_enemy: multi-investigator engagement-on-spawn requires Prey (lands in #128)"`. #128's author inherits the work of unifying the prey resolver across spawn and hunter-movement. **Resolved by `#128` (PR #149):** the reject is gone — multi-investigator spawn now suspends for the lead's `PickInvestigator` via `resolve_prey` + `spawn_engage_pending` (option A, uniform suspend through the Mythos draw loop).
- **Default spawn (`spawn: None`) goes to drawing investigator's location (`#127`, PR #135).** Per Rules Reference p.24: "If the encountered enemy has no spawn instruction, the enemy spawns engaged with the investigator encountering the card and is placed in that investigator's threat area." We model threat-area placement as `enemy.current_location = drawing investigator's location` + `engaged_with = drawing investigator` (via the same engagement resolution as Specific spawns). Future spawn-keyword expansions need to know this is the no-instruction fallback.
- **`SpawnLocation::Specific(String)` reuses `CardCode`'s namespace (`#127`, PR #135).** The decision was whether to introduce a distinct `LocationCode` newtype. Resolved: don't — locations in Arkham are cards with `ArkhamDB` codes, the namespace is shared at the data level, and introducing a newtype now would only block accidental cross-use at the engine level with no concrete consumer asking for the distinction. If a later PR has a struct that holds both kinds and needs the compiler to distinguish them, the newtype can land then. Load-bearing because future spawn-related field designs inherit the shared-namespace assumption.
- **`Location.code: CardCode` is a required field (`#127`, PR #135).** Locations now carry their printed `ArkhamDB` code alongside the in-scenario `id`. `test_location` defaults the code to `"_test_loc_{id}"` (underscore prefix can't collide with real codes); production scenario setup populates it explicitly from the location's metadata. The new field is the addressable identifier for `SpawnLocation::Specific` lookup — a #128-style "move enemy toward location code X" effect or a #56 Study-implementation would use the same field.
- **`state.next_enemy_id: u32` mints fresh enemy ids (`#127`, PR #135).** First engine site that spawns enemies (existing tests place by hard-coded `EnemyId(...)`). Adding a separate counter avoids conflating "enemy in play" and "card in play" id spaces — `EnemyId` and `CardInstanceId` are distinct types, mirrored by distinct counters. Bumped via `saturating_add(1)` after every mint (same pattern as `next_card_instance_id`).
- **`Event::EnemySpawned` carries `code` + `engaged_with` denormalized, and `Event::EnemyEngaged` is emitted as a paired follow-up when engagement occurs on spawn (`#127`, PR #135).** Listeners can filter on the spawned card and the engagement state from `EnemySpawned` without a separate registry lookup or `state.enemies` scan. The paired `EnemyEngaged` (fired immediately after `EnemySpawned` whenever `engaged_with: Some(_)`) means engagement-transition listeners see on-spawn engagement uniformly with mid-game engagements — Rules Reference p.10's "any time a ready unengaged enemy is at the same location as an investigator, it engages that investigator" has no on-spawn carve-out, so emitting the event matches the rule's framing. Initial review caught the omission; the paired emit landed before merge.
- **Per-enemy stat fields on `CardMetadata` deferred (`#127`, PR #135).** `spawn_enemy` hardcodes `fight: 1, evade: 1, attack_damage: 0, attack_horror: 0` because `CardMetadata` doesn't yet carry those fields — `health` is the only enemy stat present. The first Phase-7+ PR that needs combat-relevant spawned enemies inherits the work of extending `CardMetadata` and threading the new fields through `spawn_enemy`. Synthetic enemy + Phase-4 demo don't enter combat, so the deferral is safe.

- **Phase-driver pattern: each phase has a driver function (owns `PhaseStarted` as step N.1) and an end helper (owns `PhaseEnded`) (`#69`, PR #136).** #69 lands `mythos_phase` + `mythos_phase_end` (full) and `investigation_phase` (skeleton: emit + rotate to lead per Rules Reference p.24 step 2.2). #70 / #71 / the future full Investigation driver land their own peers and replace the remaining direct boundary emits in `step_phase`. `start_scenario`'s round-1 path bypasses both the Mythos driver and end helper entirely — no Mythos boundary events fire — because per Rules Reference p.24 the phase is skipped, not just empty.
- **Player-initiated phase actions are peers to action-phase actions, not `AwaitingInput` sub-choices (`#69`, PR #136).** `PlayerAction::DrawEncounterCard` sits alongside `Investigate` / `Move` / `PlayCard`. Future per-investigator phase content (Upkeep choices, Enemy responses) follows the same shape unless it's genuinely a sub-choice within a resolving effect.
- **No "end round" / "end phase" actions; `EndTurn` auto-chains across phase boundaries (`#69`, PR #136).** The chain pauses only when player input is genuinely required (Mythos 1.4 draws, future printed Fast windows that don't auto-skip). UI gets discrete pauses for free at the natural beats.
- **`open_fast_window` helper for printed-rule Fast windows (`#69`, PR #136).** Always emits `WindowOpened`; auto-skips (emits `WindowClosed` + runs continuation inline) when no reactions queue AND `any_fast_play_eligible` returns false. Push-before-scan ordering so `check_play_card`'s `permissive_window` gate sees the about-to-open window during eligibility scan. `ResolveInput::Skip` closes pure-Fast windows (no `pending_triggers`) via the top-of-stack index when no reaction-driven window is pending. Eligibility uses extracted `check_play_card` / `check_activate_ability` validators so the gate is the real PlayCard / ActivateAbility gate. #70 / #71 / future Investigation-driver PRs use this helper.
- **Eliminated investigators skip Mythos 1.4 draws and Investigation 2.2 turn rotation (`#69`, PR #136).** Per Rules Reference p.10 (Elimination), eliminated investigators (Status::Killed / Insane / Resigned) interact with the game only via per-investigator-value calculations. `mythos_phase` seeds `mythos_draw_pending` to the first `Status::Active` investigator in `turn_order`; `advance_mythos_draw_pending` skips eliminated ones; `investigation_phase` rotates to the first `Status::Active` investigator. The same predicate applies at all three sites.

- **Upkeep driver mirrors Mythos, inverted around the window position (`#70`, PR #142).** Mythos's player window sits at the END of its phase (post-1.4); Upkeep's sits at the START (post-4.1). So `upkeep_phase` opens the post-4.1 window immediately and `upkeep_resume` is the *content* continuation (4.2–4.5), with `upkeep_phase_end` as the `mythos_phase_end` analog (`PhaseEnded` + transition). Future phase drivers with a *leading* window follow this shape; *trailing* windows follow Mythos's.
- **Action refresh relocated from `rotate_to_active` to Upkeep 4.2 (`reset_actions`) (`#70`, PR #142).** `rotate_to_active` is set-active-only now; `start_scenario` seeds round 1 via the same helper. `ActionsRemainingChanged` fires once per round at Upkeep (+ round-1 seed), no longer at each turn-start. A future "turn-start" consumer needs a new `TurnStarted` event, not this signal.
- **Round-counter increment relocated from `step_phase` into `mythos_phase` step 1.1 (`#70`, PR #142).** Step 1.1 is the rule's "round begins" point (Rules Reference p.24: *"the first framework event of the round … formalizes the beginning of a new game round"*). Future `RoundStarted` / `RoundEnded` event emits land here (and at `start_scenario` for round 1) when a card consumer forces them. Round 1 stays special-cased in `start_scenario`.

- **Per-investigator `WindowKind::BeforeInvestigatorAttacked` + final `AfterAllInvestigatorsAttacked` (bare variants), cursor on `GameState::enemy_attack_pending` (`#71`, PR #145).** Mirror of `mythos_draw_pending` rather than a payload-carrying `WindowKind`. The bare-variant shape preserves the consolidation-into-single-variant follow-up's option space (`#140`); the per-investigator window subsumes the rules' "return to the previous player window" reading without inventing a separate inter-step window. Future per-investigator phase loops (e.g., per-step-2.2 windows in `#137`) inherit this shape: bare `WindowKind` variants + a cursor field on `GameState`, advanced inside the window-close continuation.
- **`resolve_attacks_for_investigator`'s early-break on `Status != Active`, with `#144` queued for the full Elimination flow (`#71`, PR #145).** Rules-correct minimal interpretation: after the investigator is defeated by attack N, remaining engaged enemies don't attack (per Rules p.10 step 3, they disengage) and don't exhaust (per Rules p.25, exhaust is part of "completion of dealing the attack"). The early-break implements this without modeling the disengage half. `#144` lands the full disengage + re-engage flow (blocked on `#128` for prey-based multi-investigator re-engagement). `#144`'s author needs to know the early-break stays as the simpler form even after their PR clears `enemy.engaged_with` — it becomes one redundant check that does no harm. `TODO(#144)` is in-source at the call site for grep.

- **Investigation phase begins at mulligan completion, not in `start_scenario` (`#137`, PR #148).** The round-1 `PhaseStarted(Investigation)` + lead-active now fire when the mulligan window closes (in `apply_player_action`), not during `StartScenario` — Rules Reference p.27: there are no action windows during setup; the game begins after mulligans, and round 1 skips Mythos (p.24). So `StartScenario` leaves `active_investigator = None` and emits no `PhaseStarted`; the post-2.1 window must not open during setup. Setup-adjacent work inherits this kickoff point. `#147` will swap the kickoff trigger from the `mulligan_window` flip to the `mulligan_pending` cursor reaching `None`.
- **`step_phase` emits no `PhaseEnded` (`#137`, PR #148).** With Investigation owning its emit via `investigation_phase_end`, all four phases' `*_end` helpers own their boundary emit and `step_phase`'s conditional emit was deleted outright (not just extended to suppress Investigation). A new phase driver must add its own `*_end` helper — there is no `step_phase` fallback.
- **No-active-investigator Investigation parks; scenario-end is `#144` (`#137`, PR #148).** The `InvestigationBegins` continuation does nothing when no `Status::Active` investigator exists (reachable when the last investigator is defeated mid-cascade). Auto-advancing would loop the round forever — Investigation is the cascade's only natural pause point. The rules-correct handling (no remaining players → scenario ends, Rules Reference p.10 step 6) is event-only today (`check_all_defeated` emits `AllInvestigatorsDefeated`); the scenario-end consequence lands with `#144`, after which this branch is unreachable. `TODO(#144)` is in-source at the park site.

- **`hunter` + `prey` are runtime `Enemy` fields, not `CardMetadata` (`#128`, PR #149).** `Enemy.hunter: bool` + `Enemy.prey: Prey` live on the runtime enemy; the pipeline/`CardMetadata` are untouched and `spawn_enemy` defaults spawned enemies to `hunter: false, prey: Prey::Default`. No generated enemy is a runtime hunter yet. The first real spawning hunter (Phase-7+ Ghoul Priest, `Prey – Highest [combat]`) inherits extending `CardMetadata` with the two fields and threading them through `spawn_enemy` — mirrors `#127` deferring the pipeline side until a consumer exists.
- **Movement is one `PickLocation`, engagement one `PickInvestigator` (`#128`, PR #149).** The chosen prey doesn't persist (nothing on the enemy records "who am I hunting"), so movement collapses to a single destination pick over the prey-filtered shortest-step union — `hunter_destinations` filters to the equidistant-*nearest* set **before** applying prey (RR p.12: prey breaks a distance tie, never overrides distance), so picking a location is outcome-equivalent to picking an investigator-then-path. Engage-on-arrival stays a `PickInvestigator`. `resolve_prey` (→ `One` / `Tie` / `None`) is shared across all three choice points (move / engage-on-arrival / engage-on-spawn); `Tie` defers to the lead investigator.
- **Spawn ties suspend the Mythos draw loop via a separate cursor (option A, `#128`, PR #149).** `spawn_engage_pending: Option<SpawnEngagePending>` is distinct from `hunter_move_pending` because spawn resume re-enters the Mythos encounter-draw chain, not the Enemy-phase hunter loop — conflating them would misroute resume. The draw loop was extracted into `run_mythos_draw_chain` (shared by the fresh draw and the post-suspend resume), carrying `surge` + `chain_count` across the input round-trip so the `MAX_SURGE_CHAIN` budget stays continuous. The two cursors are mutually exclusive (different phases); a `debug_assert!` in `resolve_input` documents it. A future card-triggered `EncounterCardRevealed` for the *current* Mythos cursor would need an explicit in-chain flag (commented in-source; safe today since nothing emits one).
- **`step_phase` returns `EngineOutcome` (`#128`, PR #149).** Only the Investigation→Enemy transition can suspend (a hunter tie, propagated out through `investigation_phase_end` → `end_turn`); the other three transitions run their driver and return `Done`, with `debug_assert_eq!(outcome, Done)` at each non-Enemy caller documenting the invariant. A future phase whose transition can suspend must thread its outcome the same way.

- **`apply_investigator_defeat` stays a synchronous `fn`; the re-engagement prey tie auto-picks the lead (`#144`, PR #152).** Unlike `#128`'s hunter/spawn ties (which suspend via `AwaitingInput` for the lead's `PickInvestigator`), elimination step 3's re-engagement tie engages `turn_order`-first **without** suspending. The deciding constraint: `apply_investigator_defeat` is called deep inside `enemy_attack`, attacks-of-opportunity (mid-action), and `take_horror` (deck-out) — making it suspend would force threading `EngineOutcome` through all of those plus solving mid-attack-loop resumption, for a tie that's only reachable in **multiplayer** (Phase 8+). The interactive choice is filed as `#151`. A future PR that wants the lead to actually choose must either thread the outcome through those three call paths or restructure the defeat chokepoint — don't assume `reengage_at_location` can simply start returning `AwaitingInput`.

- **Toy scenario = synthetic fixture, not The Gathering.** A 1-location, 1-enemy, 1-act, 1-agenda fixture lives under `crates/scenarios/src/test_fixtures/` (gated `#[cfg(any(test, feature = "test_fixtures"))]`) and serves only as Phase-4's demo. The Gathering stays the Phase-7 content goal. Rationale: keeps Phase 4 infra-focused — synthetic content needs only the primitives, not Study / Hallway / Attic / Cellar / Parlor / Ghoul Priest / specific NotZ-I treacheries. Mirrors Phase 3's "build minimal infra each card needs, ship the card" pattern flipped to infra-side.
- **Unified window stack (`#103` × `#52`).** `Vec<OpenWindow>` on `GameState` replaces the single `in_flight_reaction_window: Option<...>`. Each `OpenWindow` carries (a) kind/timing, (b) queue of pending reaction triggers, (c) the set of investigators who may submit Fast actions during it. Rules Reference treats "player window" as the umbrella; the engine should too. `#52`'s `FinishContinuation` machinery survives — driver pushes/pops instead of manipulating an Option. Worth the refactor cost up-front because every Phase-4 phase-content PR opens windows; retrofitting twice is worse than once.
- **`#75` campaign log migrates to Phase 9.** Phase 4 demonstrates a single scenario plays setup → resolution; the typed `Fact` log + `next_scenario` orchestration only have a real consumer when Night of the Zealot lands. `apply_resolution` in Phase 4 returns a typed `Resolution` value and applies XP/trauma directly to investigators on existing state.
- **`#69` splits into three.** `#126` (Revelation DSL), `#127` (spawn rules), `#69` rescoped to "Mythos phase loop on top of those primitives." Mirrors the Phase-3 `#53` split (DSL primitive + accumulator + card consumer in three PRs).
- **`#71` splits into two.** Engagement attacks (small, reuses `enemy_attack`) separated from `#128` Hunter movement (Prey enum + BFS + PickLocation). Independent acceptance per PR.
- **`#73` stays whole.** Act + agenda + doom + threshold-advance share a state shape; splitting would mean 4 tiny PRs with duplicated context.
- **Location abilities + `#56` stay unmilestoned.** Phase-4 toy is synthetic and needs no location abilities. File "location abilities DSL surface + reveal effects" only when a location-bearing scenario forces it (likely Phase 7 Gathering); pick it up with `#56` together. Matches `#52` trigger-indexing deferral, `#63` max-1-commit deferral, `#55` elder-sign split — concrete consumer first.
- **`ScenarioModule` is `fn`-pointers + a static registry**, mirroring `CardRegistry`. No `dyn`, no `Box`. Hosts call `scenarios::install(scenarios::REGISTRY)` once at startup. Tests install only what they need. Function pointers don't serialize, so `GameState` carries a serializable `ScenarioId` and the engine looks the module up via the registry — same pattern `CardRegistry` uses with `CardCode`. Survives action-log replay.
- **Round counter folded into `#70`.** `GameState.round: u32` incremented at the start of Mythos. Lazy `usage_limit` reset from `#55` stays correct under this; the counter is load-bearing for any future round-end hook. The exact Rules Reference clause governing round boundaries should be cited verbatim in the `#70` PR.

## Open questions (settled enough to start; concrete answers come with the relevant PR)

- **Window-stack invariants.** Exact push/pop points beyond phase boundaries and the reaction points `#52` already opens. Acceptable to start with that minimal set; expand when a card forces it.
- **Resolution-fired idempotency latch.** `apply()` re-calls `detect_resolution` on every `Done` outcome with no engine-side guard (tracked as `#131`). Acceptable for Phase 4 (synthetic fixture's `apply_resolution` is a no-op) but the first real `apply_resolution` (Phase 9 XP / trauma) will stack effects unless the latch lands first. Likely shape: `GameState.resolution: Option<Resolution>` checked at the top of `fire_scenario_resolution`. Defer until Phase 9.
- **`AwaitingInput`-skip contract isn't positively tested.** The hook fires on `Done` only; `AwaitingInput` and `Rejected` both skip. `Rejected` has a positive test, `AwaitingInput` does not (the cleanest `AwaitingInput`-producer in dispatch is `PerformSkillTest` at the commit window, which needs a fuller `TestGame` setup than the existing scenario-resolution tests use). Worth adding when a future PR is already touching that area; not blocking.

## Dependencies

Phase 3 — needs the skill-test machinery, action handlers, DSL evaluator, reaction-window machinery (which `#103` then refactors into the unified stack). Specifically Phase-3 issues that gate Phase-4 work:

- `#52` reaction windows — Phase-4 phase content emits events that triggered abilities react to.
- `#54` `OnEvent` trigger — DSL extension for the above.
- `#62` player decks (already shipped) — `#70` Upkeep draws from them.
- `#67` enemy state (already shipped) — `#71` engagement attacks and `NEW-C` hunter movement rely on it.

## What "done" looks like

A custom toy scenario (1 location, 1 enemy, 1 act, 1 agenda, 1 synthetic treachery, 1 spawn-bearing enemy card) plays setup through a resolution in `crates/scenarios/tests/`:

- Engine cycles Mythos → Investigation → Enemy → Upkeep → Mythos.
- Mythos draws the encounter deck and resolves treacheries / spawns enemies; Surge handled.
- Investigation lets the investigator move, investigate, fight, evade — all reusing Phase-3 actions.
- Enemy phase: engaged enemies attack; hunters move toward Prey.
- Upkeep: ready, draw, gain resource, round counter bumps.
- `#73`: doom advances on the agenda each Mythos start; threshold met → `AgendaAdvanced`. Act advances when its condition is met; end-of-deck on either side emits `ScenarioWon` / `ScenarioLost`.
- `detect_resolution` fires at the right moment; `apply_resolution` returns a typed `Resolution` and applies XP/trauma.
- Mid-scenario state serializes + replays identically via the action log (verified by a replay test).
